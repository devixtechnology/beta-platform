{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },

  // Development-only signing key (005 research R4). The application REFUSES to start with this
  // value when the environment is Production, so committing it here cannot weaken a real
  // deployment. Production must supply Jwt__SigningKey from the environment or a secret store.
  "Jwt": {
    "SigningKey": "dev-only-do-not-use-in-production-0123456789abcdef"
  }
}
